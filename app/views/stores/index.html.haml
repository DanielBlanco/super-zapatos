%h1 Listing stores

.page-menu
  = link_to 'New Store', new_store_path, :class => 'btn btn-default'

%table.table.table-bordered
  %tr
    %th Name
    %th Address
    %th

  - if @stores.empty?
    %tr
      %td.t-center{'colspan'=>'5'} The are no stores to show.

  - @stores.each do |store|
    %tr
      %td.td-name= store.name
      %td.td-address= store.address
      %td.td-actions
        .btn-group
          = link_to store, :class => 'btn btn-default' do
            %i.icon-eye-open
            Show
          = link_to store_articles_path(store), :class => 'btn btn-default' do
            %i.icon-barcode
            Articles
          = link_to edit_store_path(store), :class => 'btn btn-default' do
            %i.icon-edit
            Edit
          = link_to store, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger' do
            %i.icon-remove-sign
            Destroy

%br

